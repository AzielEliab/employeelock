# Examples

Demo rows after `employeelock init` are generic format proof:

1. Unnamed prior process (owner blank → UNOWNED, GENESIS).
2. Records desk (owned, renamed_from = ticket queue → RENAMED).

Replace them with real work. They are not accusations.
